# X12 850 using non-default separators (| element, newline segment).
# Locks that ISA-envelope separator auto-detection works, not just *~ defaults.
Extract the PO number from this pipe-delimited EDI 850.
