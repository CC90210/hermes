# Core
python-dotenv>=1.0.0
httpx>=0.27.1
fastapi>=0.115.0
uvicorn>=0.34.0

# Email
imapclient>=3.0.0
exchangelib>=5.4.0

# PO Parsing
pdfplumber>=0.11.0
openpyxl>=3.1.0
python-docx>=1.1.0

# Database
aiosqlite>=0.20.0

# Browser automation fallback (web/Electron ERPs)
playwright>=1.48.0

# Desktop automation fallback (native Win32 ERPs — A2000)
pywinauto>=0.6.8 ; platform_system == "Windows"
Pillow>=10.0.0  # screenshots for the desktop adapter audit trail

# EDI / Label generation
reportlab>=4.0.0

# Computer control (printing, notifications, watcher, clipboard)
pywin32>=306 ; platform_system == "Windows"
plyer>=2.1
watchdog>=4.0
pyperclip>=1.9

# Demo / IDE Hermes (terminal rendering)
rich>=13.7.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
respx>=0.21.0
pytest-cov>=5.0.0
