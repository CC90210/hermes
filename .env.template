# Hermes — Environment Configuration
# Copy to .env and fill in real values. NEVER commit .env

# Ollama (local model)
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=qwen2.5:32b

# Email (Outlook/Exchange)
EMAIL_HOST=outlook.office365.com
EMAIL_USER=
EMAIL_PASSWORD=
EMAIL_IMAP_PORT=993
EMAIL_SMTP_HOST=smtp.office365.com
EMAIL_SMTP_PORT=587

# Escalation (alert emails sent to this address)
ESCALATION_EMAIL=emmanuel@lowinger.com

# A2000 POS
A2000_API_URL=
A2000_API_KEY=
A2000_MODE=mock  # mock | api | edi | playwright

# Storage
DB_PATH=./storage/lowinger.db

# Invoice / Email
COMPANY_NAME=Lowinger Distribution

# EDI output (used when A2000_MODE=edi)
EDI_OUTPUT_DIR=./storage/edi_out
EDI_SENDER_ID=LOWINGER
EDI_RECEIVER_ID=A2000

# Playwright fallback (used when A2000_MODE=playwright)
A2000_BROWSER_URL=
A2000_BROWSER_USER=
A2000_BROWSER_PASS=

# Logging
LOG_LEVEL=INFO
LOG_DIR=./logs
