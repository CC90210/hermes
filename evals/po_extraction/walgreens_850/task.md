# Inbound X12 850 purchase order (Walgreens). Extract the PO header + line items.
# The real EDI parser (adapters.po_parser._extract_text_edi) runs against fixtures/po.edi.
Extract structured fields from this EDI 850.
